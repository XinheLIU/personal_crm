# OS
.DS_Store

# gstack runtime (sessions, analytics, logs, screenshots)
.gstack/

# Claude Code — machine-specific overrides
.claude/settings.local.json

# gstack / browse dependencies (reinstall: cd .claude/skills && bun install)
.claude/skills/node_modules/

# Secrets and local env
.env
.env.*
!.env.example

# Compass / local CRM data
*.db
*.db-journal
*.sqlite
*.sqlite3

# Python
__pycache__/
*.py[cod]
.Python
.venv/
venv/
.uv/

# Node (if used at repo root)
node_modules/

# Builds and caches (root only — keep .claude/skills/browse/dist for gstack browse CLI)
/dist/
.cache/
*.log

# Editors
.idea/
*.swp
*.swo
